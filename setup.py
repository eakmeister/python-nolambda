from distutils.core import setup

setup(name = 'nolambda',
      version = '1.0',
      description = 'Create simple functions in python without the lambda keyword',
      author = 'Eric Kedaigle',
      author_email = '',
      url = '',
      py_modules = ['nolambda']
)
